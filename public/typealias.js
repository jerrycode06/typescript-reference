"use strict";
// const logDetails = (name: string, uid: string | number) => {
//   console.log(`${name} has reg. no. ${uid}`);
// };
var logDetails = function (name, uid) {
    console.log(name + " has reg. no. " + uid);
};
var greets = function (user) {
    console.log("Good Morning " + user.name);
};
